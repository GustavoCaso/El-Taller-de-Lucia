- @pictures.in_groups_of(3, false) do |group|
  .row
    - group.each do |picture|
      %li.col-lg-4.col-sm-6.col-xs-12
        = image_tag(picture.foto.url(:large))
        .box-effect
          %h3= picture.title
          %p= picture.description
